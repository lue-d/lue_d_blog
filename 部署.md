# 博客部署指南

## 服务器信息

| 项目 | 值 |
|------|-----|
| IP | `122.51.117.106` |
| 系统 | CentOS 7 |
| 域名 | `lued.top` / `www.lued.top` |
| SSL | Let's Encrypt（自动续期） |
| 网站目录 | `/var/www/blog` |

## SSH 登录

```bash
ssh root@122.51.117.106
```

## 部署 / 更新

```bash
cd ~/Desktop/blog && npm run build
rsync -avz --delete out/ root@122.51.117.106:/var/www/blog/
```

或使用快捷脚本：

```bash
./deploy.sh
```

## Nginx 相关

```bash
# 配置文件位置
/etc/nginx/conf.d/blog.conf

# 检查配置
nginx -t

# 重载配置
systemctl reload nginx

# 重启 Nginx
systemctl restart nginx
```

## 证书续期

```bash
certbot renew --dry-run  # 测试续期
certbot renew            # 手动续期
```

已配置 cron 每天凌晨 3 点自动续期。
