# 相片改造文档

## 概述

将博客从单张封面图升级为支持**多图上传 + 画廊展示 + 灯箱预览**的完整相片系统。摄影、书法、感悟三种内容类型均支持此功能，且对旧内容完全向后兼容。

## 功能清单

| 功能 | 说明 |
|------|------|
| 多图上传 | 管理后台一次选择多张图片，自动压缩为 WebP（最长边 2000px） |
| 拖拽排序 | 上传前后均可拖拽调整图片顺序 |
| 画廊展示 | 详情页以瀑布流（摄影）或网格（书法/感悟）展示所有图片 |
| 灯箱预览 | 点击图片全屏放大，支持键盘导航（← → Esc）和触摸滑动 |
| 列表缩略图 | 列表页自动使用首张画廊图作为封面，无画廊图时回退原封面 |
| 向后兼容 | 旧文章（只有 cover 无画廊图）展示完全不受影响 |
| 删除清理 | 删除文章时自动清理关联的画廊图片记录 |

## 技术方案

### 数据库

新建 `images` 表，多态关联三种内容类型（与 `comments` 表模式一致）：

```sql
CREATE TABLE images (
  id           uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  post_type    text NOT NULL,          -- 'calligraphy' | 'photography' | 'reflections'
  post_slug    text NOT NULL,
  url          text NOT NULL,
  alt_text     text DEFAULT '',
  caption      text DEFAULT '',
  sort_order   integer NOT NULL DEFAULT 0,
  created_at   timestamptz DEFAULT now()
);
```

迁移文件：[supabase/migrations/003_gallery_images.sql](supabase/migrations/003_gallery_images.sql)

### 存储

- 路径格式：`{bucket}/gallery/{post_slug}/{sort_order}-{timestamp}-{filename}.webp`
- 沿用现有三个 Storage bucket（calligraphy / photography / reflections），无需新建
- 上传前通过 Canvas API 客户端压缩：最长边 2000px，WebP 格式，质量 0.8

### 组件架构

```
┌──────────────────────────────────────────────────┐
│ 管理后台                                         │
│                                                 │
│ ContentForm ── GalleryUploader                   │
│   (封面图)       ├── 多文件选择                    │
│                  ├── 客户端压缩 (compressImage)     │
│                  ├── Supabase Storage 上传         │
│                  ├── images 表 CRUD               │
│                  └── HTML5 拖拽排序               │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ 前端展示                                         │
│                                                 │
│ 列表页 ── gallery_thumbnail ?? cover              │
│                                                 │
│ 详情页 ── ImageGallery                           │
│           ├── 摄影：CSS columns 瀑布流            │
│           ├── 书法/感悟：CSS Grid 网格            │
│           └── 点击 → Lightbox                    │
│                       ├── 全屏遮罩 + 毛玻璃        │
│                       ├── 左右箭头 / 键盘导航      │
│                       ├── 触摸滑动                │
│                       └── 页码指示器 + 说明文字     │
└──────────────────────────────────────────────────┘
```

## 文件变更

### 新建（6 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `supabase/migrations/003_gallery_images.sql` | 36 | 数据库迁移 SQL |
| `lib/gallery.ts` | 86 | 服务端数据层（supabaseAdmin） |
| `lib/gallery-client.ts` | 138 | 客户端数据层（anon key） |
| `components/GalleryUploader.tsx` | 300 | 管理后台多图上传 + 拖拽排序 |
| `components/ImageGallery.tsx` | 82 | 前端画廊展示组件 |
| `components/Lightbox.tsx` | 195 | 自定义灯箱组件 |

### 修改（12 个）

| 文件 | 改动 |
|------|------|
| `lib/content-supabase.ts` | 接口 + 列表查询批量获取首张画廊图 |
| `lib/content-supabase-client.ts` | 同上（客户端版本） |
| `lib/content.ts` | 接口新增 `gallery_thumbnail` 字段 |
| `components/ContentForm.tsx` | 嵌入 GalleryUploader，提交时同步画廊图 |
| `app/admin/[type]/edit/edit-client.tsx` | 编辑时加载已有画廊图 |
| `app/admin/[type]/list-client.tsx` | 显示画廊图数量，删除时清理关联记录 |
| `app/photography/[slug]/detail-client.tsx` | 画廊 + 灯箱 |
| `app/calligraphy/[slug]/detail-client.tsx` | 同上 |
| `app/reflections/[slug]/detail-client.tsx` | 同上 |
| `app/photography/page.tsx` | 缩略图回退逻辑 |
| `app/calligraphy/page.tsx` | 同上 |
| `app/reflections/page.tsx` | 同上 |

## 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 数据库 | 独立 `images` 表 | 多态关联 + 排序/标题元数据，与 `comments` 模式一致 |
| 灯箱 | 自定义组件 | 零依赖，150 行，匹配 ink 极简美学 |
| 排序 | HTML5 Drag & Drop | 无需额外库，体验直观 |
| 压缩 | 复用 `compressImage` | 与封面图上传共享同一套压缩逻辑 |
| 依赖 | 零新增 npm 包 | 全部基于现有 next/react/supabase-js + 浏览器 API |

## 使用方式

### 创建新文章时添加画廊图

1. 进入管理后台 → 选择内容类型 → 点击"新增"
2. 填写标题、日期、描述等基本信息
3. 上传封面图（与之前相同）
4. 在"📷 作品图片"区域点击"+ 添加图片"
5. 选择多张图片（支持 JPG/PNG/WebP）
6. 拖拽调整图片顺序
7. 点击"创建"提交

### 编辑已有文章管理画廊图

1. 进入管理后台 → 选择内容 → 点击"编辑"
2. 已有画廊图会自动加载显示
3. 可新增图片、删除已有图片、拖拽重新排序
4. 点击"更新"保存

### 前端浏览

- **列表页**：自动使用首张画廊图作为封面缩略图
- **详情页**：封面图下方展示完整画廊
  - 摄影类：瀑布流布局，图片以实际比例展示
  - 书法/感悟类：等宽网格布局
- **点击任意图片**：全屏灯箱预览
  - 键盘 ← → 切换图片
  - Esc 或点击遮罩关闭
  - 移动端支持左右滑动

## 部署步骤

1. **数据库迁移**：在 Supabase SQL Editor 执行 `supabase/migrations/003_gallery_images.sql`
2. **重新构建**：`npm run build`（静态导出）
3. **部署**：将 `out/` 目录部署到服务器

无需修改 Storage bucket 配置或 RLS 策略，现有策略已覆盖 "公开可读 + 认证可写"。
