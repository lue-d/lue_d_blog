# 博客纯静态改造计划

> 目标：构建时从 Supabase 一次性拉取所有数据打进静态 HTML，运行时零数据库请求，彻底解决加载慢的问题。

---

## 一、改造概览

### 核心思路

```
改造前：
  构建时 → generateStaticParams 取 slugs → 生成空壳 HTML
  运行时 → useEffect → Supabase 请求 → 渲染内容（慢 🐢）

改造后：
  构建时 → 取 slugs + 取全部内容数据 → 数据随 HTML 一起输出
  运行时 → HTML 自带内容 → 瞬间渲染（快 🚀）
  例外：留言（动态内容）保留客户端请求
```

### 改动范围一览

| 模块 | 改动 | 说明 |
|------|------|------|
| 首页 `app/page.tsx` | 🔄 重写 | 客户端 → 服务端组件 |
| 列表页 ×3 | 🔄 重写 | 客户端 → 服务端组件 |
| 详情页 ×3 | 🔄 重写 | 数据在服务端获取，传 props 给客户端组件 |
| StatsBar | 🔄 改造 | 接收 props 而非自行 fetch |
| 管理后台仪表盘 | ➕ 新增按钮 | "发布更新"触发 GitHub Actions |
| GitHub Actions | ➕ 新增触发器 | `workflow_dispatch` 支持手动触发 |
| `lib/content-supabase-client.ts` | 🗑️ 删除公开读取函数 | 不再需要客户端请求 Supabase |
| `lib/gallery-client.ts` | ✂️ 裁剪 | 只保留管理端 CRUD 函数 |

---

## 二、详细步骤

### 步骤 1：GitHub Actions 增加手动触发

**文件**：`.github/workflows/deploy.yml`

在第 3-4 行之间加入 `workflow_dispatch`：

```yaml
on:
  workflow_dispatch:          # ← 新增：允许手动触发
  push:
    branches: [master]
```

这样你可以在 GitHub 网页上手动点按钮触发构建，也可以通过 API 调用。

---

### 步骤 2：首页改造 —— 构建时取数据

**文件**：`app/page.tsx`

当前是 `"use client"` 组件，`useEffect` 里请求 Supabase。改为**服务端组件**，数据在构建时获取。

**改造要点**：

```tsx
// app/page.tsx（改造后）
import { getContentList } from "@/lib/content-supabase";
import { getCommentStatsServer } from "@/lib/comments-server"; // 新增
import HomeClient from "./home-client"; // 客户端渲染组件

export default async function Home() {
  // 构建时并行获取所有数据
  const [calligraphies, photos, reflections, stats] = await Promise.all([
    getContentList("calligraphy"),
    getContentList("photography"),
    getContentList("reflections"),
    getCommentStatsServer(),
  ]);

  return (
    <HomeClient
      calligraphies={calligraphies.slice(0, 3)}
      photos={photos.slice(0, 3)}
      reflections={reflections.slice(0, 3)}
      stats={stats}
    />
  );
}
```

**新建文件**：`app/home-client.tsx`

把原来 `page.tsx` 里的 UI 渲染逻辑搬过来，改为 `"use client"`，通过 props 接收数据：

```tsx
"use client";

export default function HomeClient({
  calligraphies,
  photos,
  reflections,
  stats,
}: {
  calligraphies: ContentMeta[];
  photos: ContentMeta[];
  reflections: ContentMeta[];
  stats: { totalPhotography: number; totalCalligraphy: number; totalComments: number };
}) {
  // 原来 page.tsx 的 UI 渲染逻辑
  // 去掉 useState + useEffect 数据获取
  // 直接用 props 数据渲染
}
```

---

### 步骤 3：列表页改造 ×3

三个列表页（`app/calligraphy/page.tsx`、`app/photography/page.tsx`、`app/reflections/page.tsx`）结构完全一样，改造方式也相同。

**当前**：`"use client"` → `useEffect` → `getContentListClient(type)`

**改造后**：

```tsx
// app/calligraphy/page.tsx（改造后）
import { getContentList } from "@/lib/content-supabase";
import ListClient from "./list-client";

export default async function CalligraphyPage() {
  const items = await getContentList("calligraphy");
  return <ListClient items={items} type="calligraphy" />;
}
```

**新建文件**：`app/calligraphy/list-client.tsx`

把原 `page.tsx` 的 UI 部分搬过来，改为 `"use client"`，数据从 props 来。去掉 loading skeleton（构建时数据已经有了）。

摄影和感悟页面同理，各新建对应的 `list-client.tsx`。

---

### 步骤 4：详情页改造 ×3

这是最关键的改造。目前详情页结构是：

```
page.tsx（服务端壳）
  → generateStaticParams() 取 slugs
  → 渲染 detail-client.tsx

detail-client.tsx（"use client"）
  → useEffect 里并行请求：
    - getContentDataClient(type, slug)   // 文章内容
    - getGalleryImagesClient(type, slug) // 画廊图片
  → 然后再请求 getContentListClient(type) // 相关推荐
```

**改造后**：

```tsx
// app/calligraphy/[slug]/page.tsx（改造后）
import { getContentList, getContentData } from "@/lib/content-supabase";
import { getGalleryImages } from "@/lib/gallery";
import CalligraphyDetailClient from "./detail-client";

export async function generateStaticParams() {
  // 保持不变
}

export default async function CalligraphyDetailPage({ params }) {
  const { slug } = await params;

  // 构建时并行获取：文章内容 + 画廊图片 + 相关推荐
  const [content, gallery, allPosts] = await Promise.all([
    getContentData("calligraphy", slug),
    getGalleryImages("calligraphy", slug),
    getContentList("calligraphy"),
  ]);

  // 计算相关推荐（原来在客户端做）
  const relatedPosts = computeRelated(allPosts, slug, content?.meta.category);

  return (
    <CalligraphyDetailClient
      slug={slug}
      initialData={content}
      initialGallery={gallery}
      initialRelatedPosts={relatedPosts}
    />
  );
}

// 辅助：计算相关推荐
function computeRelated(all, currentSlug, category) {
  const sameCategory = all.filter(
    (item) => item.slug !== currentSlug && item.category === category
  );
  const others = all.filter(
    (item) => item.slug !== currentSlug && item.category !== category
  );
  return [...sameCategory, ...others].slice(0, 3);
}
```

**改造 `detail-client.tsx`**：

```tsx
"use client";

export default function CalligraphyDetailClient({
  slug,
  initialData,        // ← 从 props 接收，不再 fetch
  initialGallery,     // ← 从 props 接收
  initialRelatedPosts,// ← 从 props 接收
}) {
  // 去掉 useState + useEffect 数据获取
  // 直接用 initialData / initialGallery / initialRelatedPosts
  // Carousel、Lightbox 等交互组件保持不变（仍需客户端）

  // 404 情况：initialData === null
  if (!initialData) {
    return <NotFound />;
  }
  // ... 渲染
}
```

摄影和感悟详情页同理。

---

### 步骤 5：StatsBar 改造

**当前**：`"use client"` 组件，`useEffect` 里调 `getCommentStats()`

**改造后**：接收 props，纯展示组件

```tsx
"use client";

export default function StatsBar({
  totalPhotography,
  totalCalligraphy,
  totalComments,
}: {
  totalPhotography: number;
  totalCalligraphy: number;
  totalComments: number;
}) {
  // 直接渲染，不需要 useState / useEffect
}
```

调用方（HomeClient）传入数据即可。

---

### 步骤 6：新增服务端统计函数

**新建文件**：`lib/comments-server.ts`

StatsBar 原来用的 `getCommentStats()` 在 `lib/comments-client.ts`（使用 anon key）。构建时需要改用 service_role key：

```ts
import "server-only";
import { supabaseAdmin } from "./supabase-admin";

export async function getCommentStatsServer() {
  const [photoRes, calliRes, commentRes] = await Promise.all([
    supabaseAdmin.from("photography").select("*", { count: "exact", head: true }).eq("published", true),
    supabaseAdmin.from("calligraphy").select("*", { count: "exact", head: true }).eq("published", true),
    supabaseAdmin.from("comments").select("*", { count: "exact", head: true }).eq("published", true),
  ]);

  return {
    totalPhotography: photoRes.count ?? 0,
    totalCalligraphy: calliRes.count ?? 0,
    totalComments: commentRes.count ?? 0,
  };
}
```

---

### 步骤 7：管理后台加"发布更新"按钮

**文件**：`app/admin/page.tsx`（仪表盘）

在仪表盘页面新增一个"发布更新"按钮，点击后触发 GitHub Actions 重新构建部署。

**方案 A（推荐）—— GitHub API 直接触发**：

```tsx
async function triggerDeploy() {
  const token = prompt("请输入 GitHub Personal Access Token：");
  if (!token) return;

  const res = await fetch(
    "https://api.github.com/repos/{owner}/{repo}/actions/workflows/deploy.yml/dispatches",
    {
      method: "POST",
      headers: {
        Authorization: `Bearer ${token}`,
        Accept: "application/vnd.github+json",
      },
      body: JSON.stringify({ ref: "master" }),
    }
  );

  if (res.ok) {
    alert("✅ 已触发部署，约 1-2 分钟后生效");
  } else {
    alert("❌ 触发失败，请检查 Token 权限");
  }
}
```

> GitHub Personal Access Token 创建方法：GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens → 勾选仓库 + Workflows 权限。

**方案 B（更安全）—— Supabase Edge Function 代理**：

在 Supabase 项目中创建一个 Edge Function 持有 PAT，前端只调用这个 Function。不过多一步配置，对个人博客来说方案 A 足够了。

---

### 步骤 8：清理不再需要的代码

#### 可删除的文件/函数

| 文件 | 处理 | 原因 |
|------|------|------|
| `lib/content-supabase-client.ts` | 🗑️ 删除整个文件 | 公开页面不再需要客户端请求 Supabase |
| `lib/gallery-client.ts` | ✂️ 删除 `getGalleryImagesClient`、`getFirstGalleryImageClient` | 公开读取改为构建时完成 |
| 同上 | ✅ 保留 `createGalleryImage`、`updateGallerySortOrder`、`deleteGalleryImage`、`deleteAllGalleryImages` | 管理后台还需要 |

#### 可删除的组件

- 首页 `SkeletonSection` → 不再需要 loading 态
- 列表页的 skeleton → 不再需要
- 详情页的 loading skeleton → 不再需要

---

### 步骤 9：保留不变的部分

以下模块**完全不动**：

| 模块 | 原因 |
|------|------|
| `lib/supabase.ts`（anon client） | 管理后台登录、CRUD 仍需 |
| `lib/supabase-admin.ts`（service_role） | 构建时取数据 + generateStaticParams 仍需 |
| `lib/content-supabase.ts` | 构建时取数据，继续使用 |
| `lib/gallery.ts` | 构建时取画廊图，继续使用 |
| `lib/comments-client.ts` | 留言读取和提交，保留 |
| `lib/comments-admin.ts` | 留言管理，保留 |
| `components/CommentSection.tsx` | 留言区仍需客户端请求 Supabase |
| `app/admin/*` 全部页面 | 管理后台不变 |
| Supabase Auth 登录流程 | 不变 |

---

## 三、数据流对比

### 改造前

```
┌─────────────────────────────────────────────────────┐
│ 构建时（GitHub Actions）                              │
│   generateStaticParams → Supabase 取 slugs           │
│   生成空壳 HTML（无内容数据）                          │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ 运行时（浏览器）                                      │
│   首页 mount → useEffect → 3 次 Supabase 请求        │
│   列表页 mount → useEffect → 1 次 Supabase 请求       │
│   详情页 mount → useEffect → 3 次 Supabase 请求       │
│   StatsBar mount → useEffect → 1 次 Supabase 请求    │
│                                                      │
│   每次页面切换 = 1-3 次海外请求 = 300-800ms 等待       │
└─────────────────────────────────────────────────────┘
```

### 改造后

```
┌─────────────────────────────────────────────────────┐
│ 构建时（GitHub Actions）                              │
│   generateStaticParams → Supabase 取 slugs           │
│   首页服务端组件 → 并行取 3 个列表 + 统计数据          │
│   列表页服务端组件 → 各取对应列表                      │
│   详情页服务端组件 → 并行取内容 + 画廊 + 相关推荐      │
│   所有数据打入静态 HTML ✅                             │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ 运行时（浏览器）                                      │
│   任何页面渲染 → HTML 自带内容 → 瞬间展示 🚀           │
│   仅留言区 → useEffect → 1 次 Supabase 请求           │
│                                                      │
│   页面切换 = 0 次内容请求 = 瞬间                       │
└─────────────────────────────────────────────────────┘
```

---

## 四、注意事项

### 1. 构建时间会增加

之前构建只取 slugs（轻量查询），现在要取全部文章内容 + 画廊图片信息。文章数量不多的话（几十篇），影响可以忽略。如果文章上百篇，构建时间可能从 30 秒增加到 1-2 分钟。

### 2. Supabase service_role key 安全

`SUPABASE_SERVICE_ROLE_KEY` 只在 GitHub Actions Secrets 和 `.env.local` 中存在，不会暴露到客户端。服务端组件在构建时使用它，构建产物（静态 HTML/JS）中不包含该 key。

### 3. 留言仍是动态的

`CommentSection` 保留客户端请求 Supabase，因为留言是用户实时提交的，无法在构建时预生成。但只有进入详情页滚动到留言区才会触发请求，对首屏体验没有影响。

### 4. 内容更新流程的变化

```
改造前：管理后台改完 → 读者刷新即见
改造后：管理后台改完 → 点"发布更新" → 等 1-2 分钟构建 → 读者看到新内容
```

对个人博客来说这个延迟完全可接受。

### 5. 不要同时保留两套数据获取

改造后务必删除 `lib/content-supabase-client.ts`，避免后续开发时混淆。如果某天想回到原来的模式，git 历史可以恢复。

---

## 五、实施顺序建议

按依赖关系，建议按以下顺序实施：

```
1. GitHub Actions 加 workflow_dispatch        （独立，先做）
2. 新建 lib/comments-server.ts                 （独立，先做）
3. 改造 StatsBar                               （依赖步骤 2）
4. 改造首页 page.tsx + 新建 home-client.tsx     （依赖步骤 3）
5. 改造列表页 ×3                               （独立）
6. 改造详情页 ×3                               （独立）
7. 管理后台加"发布更新"按钮                     （依赖步骤 1）
8. 删除 content-supabase-client.ts             （最后，确认一切正常后）
9. 删除各组件中不再需要的 loading skeleton      （最后清理）
```

每完成一步可以 `npm run dev` 验证（开发模式下不会静态导出，服务端组件仍然动态渲染，可以正常测试）。
