# 博客国内服务器部署指南

## 当前架构回顾

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Next.js 16 `output: "export"` | 纯静态 HTML/CSS/JS，不依赖 Node 运行时 |
| 数据库 | Supabase (PostgreSQL) | 存储文章、评论、图片元数据 |
| 存储 | Supabase Storage | 图片文件（封面 + 画廊） |
| 认证 | Supabase Auth | 管理后台登录 |
| 当前托管 | GitHub Pages | 免费，但国内访问慢 |

**核心优势**：静态导出意味着前端只需要一个能 serve HTML 的服务器，不需要 Node.js 运行时，部署极其简单。

---

## 一、ICP 备案（绕不开的第一步）

国内服务器必须备案，流程大约 **2-4 周**：

| 步骤 | 耗时 | 说明 |
|------|------|------|
| 购买域名 | 1 天 | 建议 `.com` / `.cn`，实名认证 |
| 购买服务器 | 1 天 | 备案需要服务器（轻量应用服务器即可） |
| 提交备案申请 | 1-3 天审核 | 在云服务商控制台提交，需身份证、手持照片 |
| 管局审核 | 7-20 天 | 各省通信管理局，等待即可 |
| 公安备案 | 备案通过后 | 30 天内完成，较简单 |

**建议**：提前买好域名和服务器，备案可以和开发并行。阿里云/腾讯云都有专门的备案管家服务。

---

## 二、服务器选择

因为是静态博客（无 Node 运行时），不需要高配服务器。对比几个方案：

### 方案 A：轻量应用服务器（推荐）

| 服务商 | 最低配置 | 价格 | 带宽 |
|--------|---------|------|------|
| 阿里云轻量 | 2C1G | ~60 元/月 | 3-5 Mbps |
| 腾讯云轻量 | 2C2G | ~50 元/月 | 4-6 Mbps |
| 京东智联云 | 2C1G | ~40 元/月 | 3 Mbps |

这套配置跑 Nginx serve 静态文件绰绰有余，月付 ~50 元。

### 方案 B：对象存储 + CDN（更便宜更快）

```
OSS/COS 存储静态文件 → CDN 加速 → 用户
```

| 服务商 | 存储费用 | CDN 流量 | 月费估算（小站） |
|------|---------|----------|----------------|
| 阿里云 OSS + CDN | 0.12 元/GB | 0.24 元/GB | ~5-10 元/月 |
| 腾讯云 COS + CDN | 0.118 元/GB | 0.21 元/GB | ~5-10 元/月 |
| 七牛云 | 0.099 元/GB | 0.29 元/GB | ~5-15 元/月 |

**方案 B 需要额外配置**：
- CDN 回源到 OSS
- 自定义域名 + SSL 证书
- 更新时需要刷新 CDN 缓存

### 方案 C：服务器 + Nginx（灵活但费钱）

如果将来想加后端功能（API、SSR），选方案 A 的基础上升级即可。静态博客现阶段方案 A 或 B 完全够用。

### 我的建议

> **首选方案 A（轻量服务器 + Nginx）**
>
> 理由：配置简单、固定成本（~50 元/月）、可以顺便跑其他小服务、有完整的服务器权限。如果以后想加 SSR 或 API 端点也不需要改架构，把 Next.js `output` 改回 `standalone` 就行。
>
> 如果纯粹追求快和便宜，选方案 B。

---

## 三、Supabase 延迟问题 ⚠️

这是最关键的问题。**Supabase 的免费库在 `ap-southeast-1`（新加坡）或 `us-east-1`**，国内直连延迟很高（200-400ms），首屏加载可能卡 2-3 秒。

### 你的选择

| 方案 | 优点 | 缺点 |
|------|------|------|
| **a. 自建 Supabase** | 数据库在国内，延迟 <10ms | 需要服务器跑 Docker，维护成本高 |
| **b. 换成国内 BaaS** | 延迟低、中文支持好 | 迁移成本、API 不兼容 |
| **c. 保持 Supabase + 优化** | 不动现有代码 | 首次加载仍慢 |
| **d. 混合：静态生成 + Supabase 仅后台用** | 前台秒开、后台照旧 | 需要改 Data Flow |

### 我的建议

> **短期：先用方案 c**，加 Loading 骨架屏（你已经有）、SWR stale-while-revalidate 缓存策略，用户体验不会太差。
>
> **长期：考虑方案 d**，前台页面在构建时静态生成（`generateStaticParams` + 构建时调 Supabase），用户访问时完全不走数据库。评论和留言功能保留客户端 Supabase 调用。
>
> 你现在的 `getContentList()` 已经有本地 MDX 回退，**思路是对的**——就是应该把 Supabase 当成"构建时数据源"而不是"运行时数据源"。这样既解决了延迟问题，又保留了 Supabase 管理后台。

---

## 四、部署方案

### 推荐架构（方案 A + Nginx）

```
用户 → 域名(DNS) → 轻量应用服务器(Nginx) → 静态文件
                                                ↓
                           Supabase(国外，仅管理后台 + 评论用)
```

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/blog;
    index index.html;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    # 静态资源强缓存
    location /_next/static {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback（如果需要）
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # 404
    error_page 404 /404.html;
}
```

### 自动化部署

```bash
# 本地或 CI 构建
npm run build    # 生成 out/ 目录

# 上传到服务器
rsync -avz --delete out/ user@server:/var/www/blog/

# 或者用 GitHub Actions（push 自动部署）
# .github/workflows/deploy.yml
```

---

## 五、HTTPS / SSL 证书

Let's Encrypt 免费证书，Certbot 自动续期：

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx

# 自动配置 Nginx + SSL
sudo certbot --nginx -d your-domain.com

# 自动续期（已内置 timer）
sudo certbot renew --dry-run
```

---

## 六、费用汇总

| 项目 | 年费（估算） |
|------|-------------|
| 域名（.com） | ~70 元 |
| 轻量应用服务器 2C2G | ~600 元 |
| SSL 证书 | 0（Let's Encrypt） |
| Supabase | 0（免费 tier，500MB DB + 1GB Storage） |
| **合计** | **~670 元/年** |

如果选方案 B（OSS + CDN），年费可以压到 **~150 元以内**。

---

## 七、推荐的实施路线

| 阶段 | 内容 | 时间 |
|------|------|------|
| **1** | 买域名 + 服务器 | 1 天 |
| **2** | 提交 ICP 备案（等待中搭环境） | 2-4 周 |
| **3** | 服务器装 Nginx + 配置 SSL | 2 小时 |
| **4** | Rsync 上传 `out/` 目录测试访问 | 30 分钟 |
| **5** | GitHub Actions 自动部署 | 1 小时 |
| **6** | 观察 Supabase 延迟，决定是否优化 | 持续 |

---

## 八、需要留意的坑

1. **备案期间服务器不能有网站内容**——阿里云/腾讯云会检查，建议备案通过后再上传文件
2. **Supabase anon key 是公开的没关系**——你的 RLS 策略已经限制了只有 `published=true` 的内容可读，anon key 本身就是公开设计的
3. **图片最好放国内对象存储**——Supabase Storage 的图片在国内加载很慢，可以考虑上传时同步传一份到阿里云 OSS，或者干脆迁移回 Supabase 自建（如果自建在国内服务器上）
4. **如果将来换 `output: "standalone"`**——需要服务器装 Node.js 18+，Nginx 做反向代理，内存建议 ≥1GB
5. **域名 DNS 提前解析到服务器 IP**——备案要求域名指向国内服务器

---

## 先聊到这里

以上是整体的部署考量。最关键的决策点是 **Supabase 延迟怎么处理**，建议先不改代码，部署上去跑几天看实际体验再决定。

有什么想法或者疑问？
